import { OutsideEventsDirective } from './outside-events.directive';

describe('OutsideEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new OutsideEventsDirective();
    expect(directive).toBeTruthy();
  });
});
